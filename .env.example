# Backend API URL
# For production (Fly.io deployment)
VITE_API_URL=https://life-review-api.fly.dev/api

# For local development (when running backend locally)
# VITE_API_URL=http://localhost:8080/api

# Optional: OpenAI API Key (only needed if calling OpenAI directly from frontend)
# VITE_OPENAI_API_KEY=sk-your-key-here
